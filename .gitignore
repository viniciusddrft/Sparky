# https://dart.dev/guides/libraries/private-files
# Created by `dart pub`
.dart_tool/

# Avoid committing pubspec.lock for library packages; see
# https://dart.dev/guides/libraries/private-files#pubspeclock.
pubspec.lock
stress_test_todo
logs.txt
PLAN.md

# Test coverage artifacts (generated by `dart test --coverage` and
# `format_coverage`; CI uploads as artifact, no need to commit).
coverage/
build/
